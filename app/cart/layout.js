export const metadata = {
  title: 'Your Cart — Diffuse Egypt',
  description: 'Review your selected items and checkout.',
  robots: { index: false, follow: false },
}

export default function CartLayout({ children }) {
  return children
}
