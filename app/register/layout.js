export const metadata = {
  title: 'Create Account — Diffuse Egypt',
  description: 'Create your Diffuse Egypt account and start shopping premium basics delivered across Egypt.',
  robots: { index: false, follow: false },
}

export default function RegisterLayout({ children }) {
  return children
}
